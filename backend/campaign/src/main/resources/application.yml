server:
  port: 8080

spring:
  application:
    name: campaign

  elasticsearch:
    uris: http://localhost:9200
    connection-timeout: 3s
    socket-timeout: 3s
    index:
      auto-create: true

  data:
    redis:
      host: localhost
      port: 6379

  datasource:
    url: jdbc:mysql://localhost:3306/campaign?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: root
    password: ssafy
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: none
    open-in-view: false

springdoc:
  swagger-ui:
    path: /swagger-ui.html